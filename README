# TesteApi

## Descrição
Este projeto é uma API de teste desenvolvida em C#. Ele serve como um exemplo para demonstrar a criação e o uso de APIs utilizando a linguagem C# e o framework .NET.

## Funcionalidades
- Criação de um api de teste com versionamento

## Requisitos
- .NET SDK 9.0 ou superior
- Visual Studio Code


## Como executar o projeto
1. Clone o repositório para sua máquina local:
    ```sh
    git clone https://github.com/seu-usuario/TesteApi.git
    ```
2. Navegue até o diretório do projeto:
    ```sh
    cd TesteApi
    ```
3. Restaure as dependências do projeto:
    ```sh
    dotnet restore
    ```
4. Execute o projeto:
    ```sh
    dotnet run
    ```

## Estrutura do Projeto
- `Controllers/`: Contém os controladores da API.
- `Program.cs`: Ponto de entrada da aplicação.

## Contribuição
Se você deseja contribuir com este projeto, por favor, siga os passos abaixo:
1. Faça um fork do repositório.
2. Crie uma branch para sua feature ou correção de bug:
    ```sh
    git checkout -b minha-feature
    ```
3. Faça commit das suas alterações:
    ```sh
    git commit -m 'Minha nova feature'
    ```
4. Envie para o repositório remoto:
    ```sh
    git push origin minha-feature
    ```
5. Abra um Pull Request.

## Licença
Este projeto está licenciado sob a licença MIT. Veja o arquivo `LICENSE` para mais detalhes.